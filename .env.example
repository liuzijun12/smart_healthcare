# ====================================
# 智能医疗系统环境变量配置文件（示例）
# ====================================
# 
# 使用说明:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改下面的配置为你的实际值
# 3. 不要将 .env 文件提交到 Git 仓库
#
# ====================================

# ============ Django 配置 ============
# Django 密钥（生产环境请使用随机字符串）
SECRET_KEY=your-secret-key-change-this-in-production

# 调试模式（生产环境设置为 False）
DEBUG=True

# 允许访问的主机（用逗号分隔，不要有空格）
# 开发环境: localhost,127.0.0.1,*
# 生产环境: 你的IP或域名，例如: 154.8.136.151,yourdomain.com
BACKEND_ALLOWED_HOSTS=localhost,127.0.0.1,vue-smart-healthcare.code-review.fun,*

# ============ 前端 Vite 配置 ============
# 前端允许访问的主机（用逗号分隔，不要有空格）
# 注意：需要重启前端容器才能生效
FRONTEND_ALLOWED_HOSTS=localhost,127.0.0.1,vue-smart-healthcare.code-review.fun,.code-review.fun

# ============ 数据库配置 ============
# 数据库引擎（MySQL）
DB_ENGINE=django.db.backends.mysql

# 数据库名称
DB_NAME=smart_healthcare

# 数据库用户名（应用连接使用）
DB_USER=healthcare_user

# 数据库密码（应用连接使用）
DB_PASSWORD=your-db-password-change-this

# 数据库主机（Docker 中使用容器名 mysql）
DB_HOST=mysql

# 数据库端口
DB_PORT=3306

# MySQL Root 密码（仅用于初始化数据库）
MYSQL_ROOT_PASSWORD=your-root-password-change-this

# ============ MinIO 配置 ============
# MinIO 管理员用户名
MINIO_ROOT_USER=minioadmin

# MinIO 管理员密码
MINIO_ROOT_PASSWORD=your-minio-password-change-this

# MinIO 服务端点（Docker 中使用容器名 minio）
MINIO_ENDPOINT=minio:9000

# MinIO 存储桶名称
MINIO_BUCKET_NAME=healthcare-images

# ============ 其他配置 ============
# 时区设置
TZ=Asia/Shanghai
TIMEZONE=Asia/Shanghai

# 语言代码
LANGUAGE_CODE=zh-hans

# ============ Django 超级用户配置 ============
# Django 管理后台超级用户（首次启动时自动创建）
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=your-admin-password-change-this
DJANGO_SUPERUSER_EMAIL=admin@example.com
